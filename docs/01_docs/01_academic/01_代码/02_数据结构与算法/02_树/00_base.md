# 概念

- 表示方法

  - 双亲表示法：存数据和父节点
  - 孩子链表：
  - 孩子兄弟：
- ASL(Average Search Length)：

  - 定义：平均查找长度，在查找过程中平均需要比较的次数。
  - 计算：
    - $ASL = \frac{1}{n} \sum_{i=1}^{n} c_i$
    - 其中，$n$为树的元素个数，$c_i$为第$i$个元素的深度，根节点为1。
